# Android / Gradle
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# 权重 50MB，由 CI 在编译时从官方 release 下载注入（本地下载极慢），不进仓库
# 说明：官方 release 的 7z 里【只有权重、没有二进制】，所以引擎二进制必须随仓库分发，
#       放在 prebuilt/ 下（1.7MB），不能加进本忽略清单。
app/src/main/assets/pikafish.nnue
